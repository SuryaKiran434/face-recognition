name: CI

on:
  push:
    branches: [main]
  pull_request:

# Least privilege for GITHUB_TOKEN. Every job in this workflow only reads the
# repository -- checkout, dependency install, lint/test and artifact upload all
# work fine with `contents: read`. A job that ever needs to write (push a commit,
# comment on a PR, cut a release) must request that scope on itself rather than
# widening this block.
permissions:
  contents: read

jobs:
  test:
    name: Tests (Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          # SonarCloud needs the full history to attribute code to authors and
          # to work out what counts as "new code"; a shallow clone breaks both.
          fetch-depth: 0

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip

      # Installed: what the tested modules actually import.
      #   numpy   -- encodings, matching, events fixtures
      #   opencv-python-headless -- replaces opencv-python: same cv2 API, no
      #              libGL system dependency on the runner
      #   pillow + pillow-heif -- `preprocessor` is the only Pillow consumer
      #              and imports pillow_heif at module scope, registering the
      #              HEIF opener; without both, tests/test_preprocessor.py
      #              would silently skip and Pillow bumps would go unverified
      #   pytest  -- runner
      # NOT installed: dlib and ultralytics. No test imports `recognizer`
      # (the only dlib consumer), and YOLO is imported lazily inside
      # DetectorImpl, so neither is reachable from tests. Installing
      # requirements.txt would compile dlib from source and pull torch for
      # no benefit.
      #   pytest-cov -- coverage reporting. Named explicitly here rather than
      #              installed via requirements-dev.txt, because that file
      #              pulls in requirements.txt (and therefore dlib).
      #
      # Versions are pinned to what run 33278499626 -- the last green run on
      # main -- already resolved, so this locks the set rather than moving it.
      # The cost is that Dependabot cannot see them: it reads manifests, not
      # ad-hoc pip command lines, so these six are manual maintenance now.
      #
      # `--only-binary :all:` makes pip refuse source distributions, so no
      # package's setup.py executes on the runner. Verified against the
      # runner's target (cp312/manylinux): all 11 distributions in the resolved
      # tree have wheels, opencv-python-headless and pillow-heif included.
      #
      # `python -m pip install --upgrade pip` used to lead this block. The run
      # log shows it doing nothing -- setup-python already provides the current
      # pip -- and pinning it would turn it into a downgrade the day the runner
      # image moves ahead, so the line is gone instead.
      - name: Install test dependencies
        run: |
          pip install numpy==2.5.2 opencv-python-headless==5.0.0.93 \
            pillow==12.3.0 pillow-heif==1.5.0 pytest==9.1.1 pytest-cov==7.1.0 \
            --only-binary :all:

      # `face_recognition_app` is the source package; everything else at the
      # repo root is tests, scripts and data.
      - name: Run pytest
        run: |
          python -m pytest -q \
            --cov=face_recognition_app --cov-report=xml --cov-report=term

      - name: Coverage summary
        run: |
          python - <<'EOF' >> "$GITHUB_STEP_SUMMARY"
          import xml.etree.ElementTree as ET
          rate = float(ET.parse("coverage.xml").getroot().get("line-rate"))
          print("## Coverage")
          print()
          print(f"**{rate * 100:.2f}%** of statements in `face_recognition_app/` covered.")
          EOF

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.xml
          if-no-files-found: error

      # Runs after pytest so coverage.xml already exists. Configuration lives in
      # sonar-project.properties at the repo root.
      #
      # Analysis is advisory, not a gate: continue-on-error keeps a SonarCloud
      # outage or a scanner hiccup from failing the required check.
      - name: SonarCloud analysis
        continue-on-error: true
        uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f # v8.2.1
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
